interface:
  display_name: "Implement Plan"
  short_description: "Implement runnable tasks safely"
  default_prompt: "Use $implement-plan only after verifying an approved canonical execution-plan artifact and its source_plan_sha256 linkage; then implement one runnable, bounded task at a time with node-level acceptance evidence."
