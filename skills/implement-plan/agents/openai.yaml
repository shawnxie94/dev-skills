interface:
  display_name: "Implement Plan"
  short_description: "Implement a plan or ready remote task"
  default_prompt: "Use $implement-plan to implement the provided plan, or read the single ready task in tasks/ready when no plan is provided."
