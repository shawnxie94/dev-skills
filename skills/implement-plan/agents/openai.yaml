interface:
  display_name: "Implement Plan"
  short_description: "Implement runnable tasks safely"
  default_prompt: "Use $implement-plan to implement the provided plan, or resolve ready tasks in tasks/ready and execute only runnable non-conflicting work."
